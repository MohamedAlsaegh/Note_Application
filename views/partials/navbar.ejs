<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <!-- App Brand / Logo -->
    <a class="navbar-brand" href="/">NoteMind</a>

    <!-- Mobile menu toggler -->
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navigation Links Container -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <!-- Align navigation to the right -->

        <!-- If no user is signed in, show Sign Up and Sign In -->
        <% if (!user) { %>
        <li class="nav-item">
          <a class="nav-link" href="/auth/sign-up">Sign Up</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/auth/sign-in">Sign In</a>
        </li>

        <!-- If user is signed in, show Notes, Profile, and Sign Out -->
        <% } else { %>
        <li class="nav-item">
          <a class="nav-link" href="/notes/show">Notes</a>
        </li>
        <li class="nav-item d-flex align-items-center">
          <a
            class="nav-link d-flex align-items-center"
            href="/users/<%= user._id %>"
          >
            My Profile
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/auth/sign-out">Sign Out</a>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
